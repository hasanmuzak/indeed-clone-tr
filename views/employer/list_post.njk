{% extends '../layout.njk' %}

{% block head %}
<title>Indeed Clone || Available Jobs</title>
{% endblock head %}

{% block content %}
{% if user.is_verified == false %}
<div oncontextmenu="return false" class="modal-box flex items-center justify-center select-none">
    <div class="w-1/3 bg-green-300 p-8 rounded text-center">
        <h3 class="text-black font-semibold text-lg">
            Eylem gerekiyor: E-mail adresinizi doğrulayın
        </h3>
        <p class="mt-6 text-gray-800">
            CV'nin işverenlere harika göründüğünden emin ol. CV'ni hemen görüntülemek ve güncellemek için e-posta adresini onayla. Hesabını onaylamak için <span class="font-semibold">{{user.email}}</span> adresine gönderdiğimiz onay bağlantısına tıkla.
        </p>
    </div>
</div>
{% endif %}

<div class="bg-white w-full pt-6 pb-4 border-t-2 list-posts-employer">
    <div class="container mx-auto flex">
        <div class="grid lg:grid-cols-4 md:grid-cols-2">
            {% for post in posts %}
            <div class="block p-2 cursor-pointer transition duration-300 hover:shadow list-post-container mt-6 border rounded mr-4">
                <a class="" href="/employer/list/posts/{{post.id}}">
                <div class="flex justify-between">
                    <h1 class="font-semibold post_title">{{post.title}}</h1>
                    <form action="/employer/post/delete/" method="post">
                        <Button html-type="submit" type="text"><Icon class="trash-icn" size="20" type="md-trash"></Icon></Button>
                    </form>
                </div>
                <h1 class="post_company">{{post.company}}</h1>
                <h1 class="post_company">{{post.location}}</h1>
                <p class="px-4 py-1 text-gray-600">
                    {{post.summary}}
                </p>
            </a>
            </div>
            {% endfor %}
            
        </div>
    </div>
</div>
{% endblock content %}